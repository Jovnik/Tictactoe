TicTacToe To Do List:
- add more classnames for the relevant squares so that theres only inside borders
- when i revert move history there should be no winner (set to false) - actually that could be a potential feature?
